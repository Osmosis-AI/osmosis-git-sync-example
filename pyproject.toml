[project]
name = "osmosis-git-sync-example"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.12.4",
    "osmosis-ai>=0.2.1",
    "rich>=14.1.0",
]
